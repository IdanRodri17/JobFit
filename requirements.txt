# ─── Core LangChain ecosystem ──────────────────────────
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-openai>=0.2,<0.3
langchain-chroma>=0.1,<0.2

# ─── LangSmith (observability + evaluation) ────────────
langsmith>=0.1,<0.4

# ─── Vector store ──────────────────────────────────────
chromadb>=0.5,<0.7

# ─── Validation & typing ───────────────────────────────
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0

# ─── Environment management ────────────────────────────
python-dotenv>=1.0

# ─── API layer (used from V4 onwards) ──────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1

# ─── Tools (V6) ────────────────────────────────────────
tavily-python>=0.3

# ─── Testing ───────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27   # for testing FastAPI endpoints
